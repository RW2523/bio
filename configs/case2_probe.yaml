inherits: ["data.yaml", "preprocess.yaml", "model.yaml"]

experiment_name: "case2_augpred_frozen_probe"

epochs: 10
batch_size: 64
num_workers: 0
lr: 0.05
weight_decay: 0.0
compute_device: "cuda"

# Path to backbone-only checkpoint saved during SSL pretraining
pretrained_backbone_path: "outputs/checkpoints/ssl/backbone_best.pt"

checkpoint_subdir: "checkpoints/case2"
seed: 42
