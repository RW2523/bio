# Exp 3 — 9-channel input + adaptive4 temporal pooling.
inherits: ["case2_e2e_finetune.yaml"]

experiment_name: "wisdm_perf_exp3_better_pooling"

feature_stack: ["raw", "delta", "window_std"]
sensor_channels: 3
in_channels: 9
pooling: adaptive4
classifier_head: linear
